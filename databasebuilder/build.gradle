import org.jetbrains.kotlin.gradle.dsl.JvmTarget


plugins {
    id "com.android.application"
    id "kotlin-parcelize"
    id "com.android.legacy-kapt"
}

android {
    compileSdk = 36

    defaultConfig {
        applicationId "com.dubproductions.databasebuilder"
        minSdk 28
        targetSdk 36
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    kotlin {
        compilerOptions {
            jvmTarget = JvmTarget.JVM_1_8
        }
    }

    buildFeatures() {
        viewBinding true
        dataBinding true
    }
    namespace 'com.dubproductions.databasebuilder'
}

dependencies {

    implementation "androidx.core:core-ktx:1.17.0"
    implementation "androidx.appcompat:appcompat:1.7.1"
    implementation "com.google.android.material:material:1.13.0"
    implementation "androidx.constraintlayout:constraintlayout:2.2.1"
    testImplementation "junit:junit:4.13.2"
    androidTestImplementation "androidx.test.ext:junit:1.3.0"
    androidTestImplementation "androidx.test.espresso:espresso-core:3.7.0"

    //ViewModel
    implementation "androidx.lifecycle:lifecycle-viewmodel-ktx:2.9.4"
    implementation "androidx.lifecycle:lifecycle-viewmodel-savedstate:2.9.4"
    implementation "androidx.lifecycle:lifecycle-livedata-ktx:2.9.4"

    //Room
    implementation "androidx.room:room-runtime:$room_version"
    kapt "androidx.room:room-compiler:$room_version"
    // Room's compiler bundles an older kotlin-metadata-jvm that can't read
    // Kotlin 2.4 metadata emitted by AGP 9's built-in Kotlin. Force a matching
    // reader onto the kapt classpath. Remove once Room ships 2.4 support.
    kapt "org.jetbrains.kotlin:kotlin-metadata-jvm:2.4.10"

    // Retrofit with Moshi Converter
    implementation "com.squareup.retrofit2:converter-moshi:3.0.0"

    // Moshi
    implementation "com.squareup.moshi:moshi-kotlin:1.15.2"

    // Nav
    implementation "androidx.navigation:navigation-fragment-ktx:$nav_version"
    implementation "androidx.navigation:navigation-ui-ktx:$nav_version"
    implementation "androidx.navigation:navigation-dynamic-features-fragment:$nav_version"
}